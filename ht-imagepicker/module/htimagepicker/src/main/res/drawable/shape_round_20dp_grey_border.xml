<?xml version="1.0" encoding="UTF-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android" >
    <!-- 圆角 -->
    <corners
        android:radius="@dimen/radius_20"
        android:topLeftRadius="@dimen/radius_20"
        android:topRightRadius="@dimen/radius_20"
        android:bottomLeftRadius="@dimen/radius_20"
        android:bottomRightRadius="@dimen/radius_20"/>

    <!-- 填充 -->
    <solid
        android:color="@color/btn_grey_border_bg"/>
    <!-- 填充的颜色 -->

    <!-- 描边，边框 -->
    <stroke
        android:width="1dp"
        android:color="@color/btn_grey_border_border"/>

</shape>
